import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ionic.comedor',
  appName: 'app-comedor',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
